# Absolute origin of the deployed backend API (no trailing slash).
# Leave EMPTY for local development — Vite's dev proxy forwards /api to :4000.
# In production set this to the backend URL, e.g.
# VITE_API_BASE_URL=https://playreview-ai-backend.onrender.com
VITE_API_BASE_URL=
